[log]
sub_dir_prefix = "log_files"
temp_dir = "log_temp_dir"        # name of temporary log file if no log file root is provided
formatter = '%(asctime)s - %(funcName)s - %(lineno)s - %(levelname)s - %(message)s'
